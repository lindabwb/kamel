services:
  - type: web
    name: controle-qualite-pcb
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app --timeout 300 --workers 1